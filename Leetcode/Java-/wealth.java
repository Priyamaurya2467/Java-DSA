package com.maurya;

public class wealth {
}
static int wealth(int [][] arr){

    int ans = Integer.MIN_VALUE;
    for (int col = 0; col < arr[row].length; col++){
        int count = 0;
        for (int n = 0; n < arr[row].length; n++){
            count = count + arr[row][n];

        }
    }
}
static int max (int[] nms){
    int max = nms[0];
    for(int i = 0; i<nms.length; i++){
        if(nms[i]>max){
            max = nms[i];
        }

    }
    return max;
}
}
